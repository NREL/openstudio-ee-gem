Add this code after line 690 (after labor_cost_multiplier retrieval):

        # Redact sensitive API key from runner results
        # OpenStudio automatically captures all argument values, so we override it
        runner.result().stepValues().append(
            openstudio.measure.OSArgument.makeStringArgument("api_key", True).tap { |arg|
                arg.setValue("<redacted>")
            }
        )

Actually, that won't work with Python. The proper way is to use the runner.result() methods.

After checking OpenStudio API, the step values are automatically populated and there's no direct way to override them from Python. The best solution is to handle this in the apply_measure.py script that generates the JSON output, or to simply not log/print sensitive values.

Let me just add a note in the code and handle it in apply_measure.py instead.
