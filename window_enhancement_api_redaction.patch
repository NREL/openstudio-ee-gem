### Patch to redact API key from apply_measure.py output

In file: lib/measures/window_enhancement/apply_measure.py

After line 296 (after collecting step_values), add:

    # Redact sensitive API key from output
    if "api_key" in step_values:
        step_values["api_key"] = "<redacted>"

This should be inserted between:
        except Exception as exc:
            step_values[sv.name()] = f"<error: {exc}>"

    ### INSERT HERE ###
    # Redact sensitive API key from output
    if "api_key" in step_values:
        step_values["api_key"] = "<redacted>"
    ### END INSERT ###

    if step_values:
        print("\nStep Values reported by measure:")
